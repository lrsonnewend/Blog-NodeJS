<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <p>{{postagem.descricao}}</p>
        <hr>
        <p>Conteúdo: {{postagem.conteudo}}</p>
        <small>Publicada em {{postagem.data}}</small>
        <hr>
    </div>
</div>